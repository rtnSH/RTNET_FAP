# 작업 내역 (Changelog) - 2026-04-19

## 배경
로그인 후 상단 앱 타이틀과 사용자 정보(Sign Out 포함) 영역의 정렬 불일치 문제를 해결하기 위해 UI 배열을 좌우(Left-Right)로 맞추는 요청 사항.

## 변경 사항
1. **백업 생성**
   - 수정을 진행하기 전 `backup/2026-04-19/` 폴더를 생성하여 `templates/` 및 `static/` 디렉토리를 안전하게 백업함.

2. **헤더 레이아웃 변경 (CSS)**
   - `static/css/style.css`에서 `header` 선택자의 속성을 `display: flex; justify-content: space-between; align-items: center;`로 변경하여 타이틀과 사용자 프로필 바가 동일한 라인에서 양쪽으로 배치되도록 수정함.
   - 불필요한 `margin-bottom`과 `.user-profile-bar`의 `margin-top`을 제거하여 깔끔하게 정렬되도록 최적화.

3. **로고 크기 조정 (HTML)**
   - `templates/index.html`에서 로그인 후 표시되는 `<h1>` 태그 내부 SVG 로고의 너비를 `240`에서 `160`으로 축소하여 모바일 화면에서도 타이틀과 로그아웃 버튼 영역이 겹치거나 줄바꿈되지 않도록 대응함. (로그인 전 화면 로고는 영향 받지 않음)


4. **사용자 프로필 영역(Sign Out 포함) 디자인 전면 개편**
   - 둥글고 큰 알약 형태(Pill-shape)의 기존 프로필 바 디자인을 제거하고, 공간을 덜 차지하며 모바일 및 터치 환경에 최적화된 미니멀한 디자인으로 변경.
   - 사용자 이름 위쪽에 미세한 라벨(`text-slate-500` 스타일의 "ENGINEER" 텍스트)을 배치하여 시각적 안정감을 줌.
   - Sign Out 버튼을 텍스트가 포함된 버튼에서, 원형의 터치 친화적인 아이콘 버튼(`icon-logout-btn`)으로 변경. 직관적인 Heroicons 라인 스타일의 아이콘을 적용하여 로고(좌측)와의 좌우 밸런스를 완벽하게 맞춤.
